function multiplyNumbers(num1, num2, num3) {
  let result = num1 * num2 * num3;
  return result;
}

let product = multiplyNumbers(2, 3, 4);

/*
arguments: 2, 3, 4
body: let result = num1*num2*num3; return result;
declaration: everything from line 1-4. the whole function definition.
function name: multipleNumbers
parameters: num1, num2, num3
return value: result
names of variales: result, product, multiplyNumbers, num1,num2,num3
*/
