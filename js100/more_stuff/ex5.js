function doSomething(string) {
  return string.split(' ').reverse().map((value) => value.length);
}

/*
 gets words from a string in an array, reverses their order, replaces them with their lengths.
*/
